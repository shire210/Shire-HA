platform: template
sensors:
  alternate_route:
    value_template: '{{ state_attr( "sensor.home_to_work_time", "distance") | replace(" mi","")|float != 23.6 }}'
    friendly_name: 'Alternate Route'
    device_class: moving
