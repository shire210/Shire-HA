platform: template
sensors:
  alternate_route:
    value_template: '{{ states.sensor.home_to_work_time.attributes.distance | replace(" mi","")|float != 23.4 }}'
    friendly_name: 'Alternate Route'
    device_class: moving
