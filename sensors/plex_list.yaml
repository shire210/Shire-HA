platform: template
sensors:
  plex_list:
    value_template: >-
      {% for attr in states.sensor.plex_watch.attributes %}
      {%- if not attr=="friendly_name" and not attr=="unit_of_measurement" %}
        {{ attr }}
      {%- endif %}
      {%- endfor -%}